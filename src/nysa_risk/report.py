"""Per-asset and per-pair parameter reports.

Consumes the outputs of :mod:`nysa_risk.parameters` and
:mod:`nysa_risk.volatility` to emit the per-collateral and per-pair
report tables described in ``docs/nysa-market-risk-framework.md`` §7
(Reporting) and the cap table described in ``docs/nysa-lb-caps.md`` §4.

Empty for now.
"""
